# ASTRA 7.29.0 / Schema 99

- paper-only submit/replace/cancel/reconcile verification;
- stale evidence, account identity, allowlist and notional gates;
- no blind retry after ambiguous broker mutations;
- residual-fill detection and fail-closed blocking;
- append-only fsync/hash-chain round-trip journal;
- generation-fenced deployment lease, controlled drain and crash-budget quarantine;
- PostgreSQL persistence schema and deterministic CI;
- external routing and live trading remain disabled.
