name: bybit-signal-outcome-audit-shards

on:
  push:
    branches:
      - agent/bybit-signal-outcome-audit-shards
    paths:
      - 'tools/audit_bybit_crypto_single_symbol_current.py'
      - '.github/workflows/bybit-signal-outcome-audit-shards.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bybit-signal-outcome-audit-shards-${{ github.ref }}
  cancel-in-progress: true

jobs:
  per-symbol-audit:
    strategy:
      fail-fast: false
      matrix:
        symbol:
          - BTCUSDT
          - ETHUSDT
          - SOLUSDT
          - XRPUSDT
          - BNBUSDT
          - DOGEUSDT
          - LINKUSDT
          - ADAUSDT
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Set up Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements.lock

      - name: Install hash-locked graph
        run: |
          python -m pip install --disable-pip-version-check --require-hashes -r requirements.lock
          python -m pip check

      - name: Lint and compile single-token audit
        run: |
          ruff check --output-format=github tools/audit_bybit_crypto_single_symbol_current.py
          python -m compileall -q tools/audit_bybit_crypto_single_symbol_current.py

      - name: Audit one frozen token from official archives
        env:
          SYMBOL: ${{ matrix.symbol }}
        run: |
          set -o pipefail
          mkdir -p "artifacts/bybit-signal-shards/$SYMBOL"
          PYTHONPATH=. python tools/audit_bybit_crypto_single_symbol_current.py \
            --symbol "$SYMBOL" \
            --lookback-days 7 \
            --opening-equity 1000 \
            --output "artifacts/bybit-signal-shards/$SYMBOL/report.json" \
            2>&1 | tee "artifacts/bybit-signal-shards/$SYMBOL/run.log"

      - name: Print one-token summary
        if: success()
        env:
          SYMBOL: ${{ matrix.symbol }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          symbol = os.environ['SYMBOL']
          report = json.loads(
              Path(f'artifacts/bybit-signal-shards/{symbol}/report.json').read_text()
          )
          all_signals = report['all_eligible_signal_events']
          trades = report['trade_outcomes']
          summary = {
              'symbol': symbol,
              'strategy_mode': report['strategy_mode'],
              'all_signal_event_count': all_signals['signal_event_count'],
              'all_signal_aggregate': all_signals['aggregate'],
              'all_signal_by_side': all_signals['by_side'],
              'all_signal_by_clarity': all_signals['by_clarity_band'],
              'closed_trade_count': trades['trade_count'],
              'trade_aggregate': trades['aggregate'],
              'trade_by_side': trades['by_side'],
              'exact_symbol_patterns': trades['exact_symbol_patterns'],
              'runner_activation_event_count': report['runner_activation_event_count'],
          }
          print('BYBIT_SIGNAL_SHARD_SUMMARY=' + json.dumps(summary, sort_keys=True))
          PY

      - name: Upload one-token evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: bybit-signal-shard-${{ matrix.symbol }}-${{ github.run_id }}
          path: artifacts/bybit-signal-shards/${{ matrix.symbol }}
          if-no-files-found: error
          retention-days: 30
