name: bybit-demo-trade-monitor-quality

on:
  push:
    branches:
      - agent/trading-quality-profit-protection
    paths:
      - 'app/execution/bybit_demo_trade_monitor.py'
      - 'tests/test_bybit_demo_trade_monitor.py'
      - '.github/workflows/bybit-demo-trade-monitor-quality.yml'
  pull_request:
    branches: [main]
    paths:
      - 'app/execution/bybit_demo_trade_monitor.py'
      - 'tests/test_bybit_demo_trade_monitor.py'
      - '.github/workflows/bybit-demo-trade-monitor-quality.yml'

permissions:
  contents: read

concurrency:
  group: bybit-demo-trade-monitor-${{ github.event_name }}-${{ github.event.pull_request.head.ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  monitor-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Set up Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements.lock

      - name: Install hash-locked graph
        run: |
          python -m pip install --disable-pip-version-check --require-hashes -r requirements.lock
          python -m pip check

      - name: Lint demo trade monitor
        run: >-
          ruff check --output-format=github
          app/execution/bybit_demo_trade_monitor.py
          tests/test_bybit_demo_trade_monitor.py

      - name: Test terminal reconciliation contract
        run: pytest -vv tests/test_bybit_demo_trade_monitor.py

      - name: Compile monitor boundary
        run: >-
          python -m compileall -q
          app/execution/bybit_demo_trade_monitor.py
          tests/test_bybit_demo_trade_monitor.py
