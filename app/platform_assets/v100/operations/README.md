# Schema 100 operations assets

This directory packages the PostgreSQL persistence boundary for paper-adapter qualification and authenticated trade-update stream evidence. It stores credential fingerprints only; raw keys and secrets are prohibited.
